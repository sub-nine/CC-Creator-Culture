server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: config-server
  profiles:
    default: local # active 없다면 local로 fallback

    group:
      local:
        - native
      docker:
        - native

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
