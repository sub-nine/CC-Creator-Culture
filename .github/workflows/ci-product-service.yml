name: CI - Product Service

on:
  push:
    branches: [main, dev]
    paths:
      - "apps/product-service/**"
      - "libs/common/**"
      - "config-repo/product-service*.yaml"
      - "config-repo/application*.yaml"
      - "build.gradle"
      - "settings.gradle"
      - "gradlew"
      - "gradlew.bat"
      - "gradle/**"
      - ".github/workflows/ci-module.yml"
      - ".github/workflows/ci-product-service.yml"
  pull_request:
    branches: [main, dev]
    paths:
      - "apps/product-service/**"
      - "libs/common/**"
      - "config-repo/product-service*.yaml"
      - "config-repo/application*.yaml"
      - "build.gradle"
      - "settings.gradle"
      - "gradlew"
      - "gradlew.bat"
      - "gradle/**"
      - ".github/workflows/ci-module.yml"
      - ".github/workflows/ci-product-service.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-product-service-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/ci-module.yml
    with:
      module-name: product-service
      gradle-task: ":apps:product-service:build"
      test-report-path: apps/product-service/build/reports/tests/
      use-postgres: true
